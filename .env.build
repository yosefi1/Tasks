# Dummy values only for "prisma generate" during build (Vercel). Not used at runtime.
DATABASE_URL=postgresql://build:build@localhost:5432/build
DIRECT_URL=postgresql://build:build@localhost:5432/build
